# deployment-templates

One-click deploys for Claude agents:
- Vercel (frontend + serverless)
- Railway (backend + Postgres)
- Modal (Python-native serverless for compute-heavy agents)
- Docker Compose (full local dev stack)

Plus a `csk deploy --target <provider>` CLI command and secrets-rotation script.

Status: skeleton only. Templates land in Week 3.
