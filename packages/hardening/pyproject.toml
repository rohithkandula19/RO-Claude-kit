[project]
name = "ro-claude-kit-hardening"
version = "0.0.1"
description = "Production hardening for Claude agents: prompt-injection defense, tool-use guardrails, output validation, observability."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ro_claude_kit_hardening"]
